# HPC Python scripts use only the Python 3 standard library (multiprocessing, threading).
# No pip packages are required for *.py files here.
# C++ sources require g++ with OpenMP; install build-essential on Ubuntu.
